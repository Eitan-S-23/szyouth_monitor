# 五 Agent 小组讨论会议摘要

**日期**: 2026-04-12  
**议题**: szyouth_monitor.py v1.0 存在的 5 个问题修复方案  
**参与者**: Agent A（架构）、Agent B（实现）、Agent C（安全）、Agent D（用户体验/文档）、Agent E（测试/验证）

---

## 一、问题清单

| # | 问题 | 严重程度 |
|---|------|----------|
| 1 | 不能同时监控全部 41 个站点 | 功能缺失 |
| 2 | 没有 README.md 文档 | 文档缺失 |
| 3 | Webhook errcode 93000 (invalid webhook url) | 配置问题 |
| 4 | 缺少反检测机制（防控机制） | 安全风险 |
| 5 | 误报: 粤港澳大湾区站点报告30天连续可预订，实际仅3天 | 致命BUG |

---

## 二、关键技术发现

在讨论开始前，主 Agent 进行了深入的技术调研，发现了问题 5 的根本原因：

### 错误的 API（v1.0 使用）

```
GET /api/hotels/{id}?come_date=X&leave_date=Y
```

返回的 `male_beds_count` 和 `female_beds_count` 是**静态床位容量**，不是实时可预订数。无论该日期是"已约满"还是"可预订"，这两个字段的值完全相同。

### 正确的 API（网站前端实际使用）

```
GET /api/hotels/{id}/day30?user_gender=1000
```

返回 `{日期: 可用床位数}`，其中 0 表示已约满，>0 表示可预订。

**验证**: 从网站前端 JS 源码 (168112e.js) 中找到关键代码：
```javascript
this.$axios.$get("hotels/" + this.id + "/day30", { params: { user_gender: 1e3 } })
// 1e3 = 1000
```

### user_gender 参数实验结果

| user_gender | 站点 75 可用天数 | 站点 85 可用天数 | 结论 |
|-------------|-----------------|-----------------|------|
| 1000 | 8/30 | 12/30 | ✅ 真实可预订数据 |
| 1001 | 30/30 | 30/30 | ❌ 返回总容量 |
| 1002 | 30/30 | 30/30 | ❌ 返回总容量 |
| 0 | 30/30 | 30/30 | ❌ 返回总容量 |
| 1 | 30/30 | 30/30 | ❌ 返回总容量 |

**结论**: 只有 `user_gender=1000` 才返回真实可预订数据。

---

## 三、各 Agent 提案摘要

### Agent A — 架构设计

- `-s` 参数支持 `all | 75,85,86 | 85` 三种格式
- 新增 `resolve_station_ids()` 分页获取全部站点
- 新增 `fetch_day30()` 替代逐天查询
- Webhook Key 支持 UUID 或完整 URL 自动提取
- 分层反检测延迟: 请求间 1-3 秒，站点间 2-5 秒，轮次间 ≥10 分钟

### Agent B — 实现细节

- 使用 `requests.Session` 保持连接
- 性能对比: 旧方案每轮 1230 次请求 → 新方案仅 41 次
- 简化 `find_consecutive_available()`，移除 gender/beds_info 参数
- 移除 `--range` 参数（/day30 固定返回 30 天）
- 移除 `--gender` 参数（user_gender=1000 不区分性别）

### Agent C — 安全/反检测

- 5 个真实 User-Agent 字符串轮换
- `smart_delay()`: 1.5-3 秒随机延迟 + 5% 概率触发 3-8 秒"思考暂停"
- 完整浏览器请求头: Referer, Origin, Sec-Fetch-*
- Webhook Key UUID 格式验证 + URL 自动提取
- `test_webhook()` 启动时测试连通性
- `mask_key()` 日志脱敏
- errcode 93000 专项错误提示

### Agent D — 用户体验/文档

- `-s` 设为必需参数（防止误操作批量查询全部站点）
- 移除 `--range`（/day30 已覆盖，冗余参数）
- 环境变量名改为 `SZYOUTH_WEBHOOK_KEY`
- 合并通知: 同一站点的多个可预订区间合并为一条消息
- 完整 README: 含快速开始、参数说明、Webhook 图文配置教程、API 说明

### Agent E — 测试/验证

- 确认 user_gender=1000 是唯一可靠值
- /day30 返回完整 30 天数据（含 0 值日期）
- 测试边界: 不足 30 天、缺失日期、403/429 处理
- 站点查询失败不应中断整轮检查
- 强调 `int()` 类型转换安全处理

---

## 四、讨论要点与分歧

### 4.1 `-s` 参数是否应设默认值？

| 立场 | Agent | 理由 |
|------|-------|------|
| 默认 `all` | A | 方便用户 |
| **必需参数** | **D, E** | **防止用户不知情下触发 41 站点全量查询** |

**决议**: `-s` 设为必需参数，用户必须显式指定 `all` 或具体站点 ID。

### 4.2 是否保留 `--gender` 参数？

| 立场 | Agent | 理由 |
|------|-------|------|
| **移除** | **B, D, E** | **/day30 API 只用 user_gender=1000 且无性别区分的床位数据** |
| 保留做显示过滤 | A | 未来可能有用 |

**决议**: 移除。`user_gender=1000` 是唯一可靠值，且 /day30 不返回分性别数据。

### 4.3 是否保留 `--range` 参数？

| 立场 | Agent | 理由 |
|------|-------|------|
| **移除** | **A, B, D** | **/day30 固定返回 30 天，参数无意义** |

**决议**: 全票移除。

### 4.4 Webhook 错误处理级别

| 立场 | Agent | 理由 |
|------|-------|------|
| 警告后继续 | A | 灵活，允许先运行后配置 |
| **测试失败即退出** | **C, D, E** | **避免用户长时间运行却收不到通知** |

**决议**: 启动时测试 Webhook，失败则报错退出。未配置 Webhook 时仅控制台输出（正常运行）。

---

## 五、最终一致方案

全部 5 位 Agent 一致同意以下方案:

### 5.1 修复误报 (Issue #5) — 最高优先级

- 弃用 `/api/hotels/{id}?come_date=X&leave_date=Y`（Detail API）
- 改用 `/api/hotels/{id}/day30?user_gender=1000`
- 硬编码 `user_gender=1000`（唯一可靠值）
- 每站点仅需 1 次 API 调用（旧方案需 30 次/站点）

### 5.2 多站点监控 (Issue #1)

- `-s` 参数类型改为 `str`，支持:
  - `all` — 分页获取全部活跃站点（status=1000）
  - `75,85,86` — 逗号分隔多个站点 ID
  - `85` — 单个站点 ID
- `-s` 设为必需参数

### 5.3 Webhook 配置修复 (Issue #3)

- `validate_webhook_key()` 函数:
  - 接受纯 UUID 格式: `xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`
  - 接受完整 URL: 自动用正则提取 `key=` 后的 UUID
  - 格式不合法时报错退出
- `test_webhook()`: 启动时发送测试消息验证连通性
- errcode 93000 专项中文提示
- `mask_key()` 日志脱敏
- 环境变量改为 `SZYOUTH_WEBHOOK_KEY`

### 5.4 反检测机制 (Issue #4)

- `APIClient` 类封装 `requests.Session`
- 5 个真实 User-Agent 随机轮换
- 完整浏览器请求头 (Referer, Origin, Sec-Fetch-*)
- `smart_delay()`:
  - 站点间: 1.5 ~ 3.0 秒随机延迟
  - 5% 概率触发 3 ~ 8 秒"思考暂停"
  - 查询失败后: 2.0 ~ 4.0 秒延迟
- 最小监控间隔: 10 分钟

### 5.5 README 文档 (Issue #2)

- 功能概述、环境要求
- 快速开始 + 完整参数表
- **Webhook 配置详细图文说明**（重点！解决 errcode 93000）
- API 技术说明（解释 v1.0 为何误报）
- 反检测机制说明
- 示例输出

### 5.6 其他改进

- 移除 `--range` 参数（冗余）
- 移除 `--gender` 参数（ /day30 不支持）
- 移除 `_summarize_beds()` 函数（不再有分性别数据）
- 移除 `fetch_hotel_detail()` 和 `check_daily_availability()` 函数
- 合并通知: 同一站点多个可预订区间合并为一条消息
- 站点查询失败不中断整轮检查
- 分页获取站点列表（API 分 4 页，共 48 个站点）

---

## 六、性能对比

| 指标 | v1.0 | v2.0 |
|------|------|------|
| 每轮 API 请求数 (全部站点) | ~1230 次 (30天 × 41站) | **41 次** (1次/站) |
| 每轮耗时 (估算) | ~20 分钟 | **~2 分钟** |
| 数据准确性 | ❌ 误报 (静态容量) | ✅ 精准 (实时可预订) |
| 支持站点数 | 1 个 | **41 个 (或自定义)** |

---

## 七、交付物

1. ✅ `szyouth_monitor.py` — 完全重写的 v2.0 脚本
2. ✅ `README.md` — 完整使用文档含 Webhook 配置教程
3. ✅ `会议摘要.md` — 本文档
